#!/bin/zsh

set -euo pipefail

SCRIPT_DIR=${0:A:h}
export PYTHONPATH="${SCRIPT_DIR}/src${PYTHONPATH:+:${PYTHONPATH}}"

if command -v python3.13 >/dev/null 2>&1; then
  exec python3.13 -m fits2fil "$@"
fi

exec python3 -m fits2fil "$@"
